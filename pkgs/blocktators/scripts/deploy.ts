import hre from 'hardhat'

async function main() {
  // todo wire up the deployment scripts based on the truffle examples
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error)
    process.exit(1)
  })
